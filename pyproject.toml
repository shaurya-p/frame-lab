[project]
name = "frame-lab"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy"]

[dependency-groups]
dev = [
    "pytest>=8",
    "pytest-timeout>=2",
    "ruff>=0.4",
]

[tool.pytest.ini_options]
timeout = 2

[tool.ruff]
target-version = "py310"
